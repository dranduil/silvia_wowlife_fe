export const getters = {
    GET_GENERAL_SETTINGS: (state) => {
        return state
    },

    GET_LOADING_TEXT: (state) => {
        return state
    },

    GET_MENU_LINKS: (state) => {
        return state.menu
    }
}
